@using LibraryBooks.Dependencies
@using LibraryBooks.Entities;
@using LibraryBooks.PL.WebPages.Models;

@{
    Layout = "~/Pages/Layout/_BasicLayout.cshtml";

    var usersBll = DependencyResolver.Instance.UserInfoLogic;
    var accountBll = DependencyResolver.Instance.AccountLogic;

    //var usersAndAwards = DependencyResolver.Instance.UsersAndAwardsLogic;

    var user = usersBll.GetById(int.Parse(Request["id"]));

    //var awards = usersAndAwards.GetAwardsByUser(user);
    PageData["Title"] = "User Deteils";

    if (!(User.IsInRole("admin") || accountBll.GetAuthDataByLogin(User.Identity.Name).Id == user.UserId))
    {
        Response.Redirect("/");
    }
}


<h3>
    Читательский : @user.FullName
</h3>


<div class="card" style="width: 300px;">
    <div class="card-body">
        <p class="card-text">
            Age : @user.Age
        </p>
        <p class="card-text">
            DateOfBirth : @user.DateOfBirth.ToString("D")
        </p>
        <p class="card-text">
            PhoneNumber : @user.Phone
        </p>
        <a class="btn btn-primary" href="~/Pages/User/Edit?id=@(user.UserId)">Edit</a>
        <h5>
            List Books
        </h5>

        @*@if (User.IsInRole("admin"))
        {
            <a class="btn btn-primary" href="~/Pages/User/AssignAward?id=@(user.Id)">Add</a>
        }
        @if (awards.Count > 0)
        {

            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col">Title</th>
                        <th scope="col"></th>
                    </tr>
                </thead>
                <tbody>
                    @{
                        int index = 0;
                        foreach (var award in awards)
                        {
                            index++;
                            <tr>
                                <th scope="row">@index</th>
                                <td>@award.Title</td>
                                <td>
                                    @if (User.IsInRole("admin"))
                                    {
                                        <a class="btn btn-primary unAssign" href="~/Pages/User/UnAssignAward?idUser=@user.Id&idAward=@(award.Id)">Delete</a>
                                    }

                                </td>
                            </tr>
                        }
                    }
                </tbody>
            </table>
        }
        else
        {
            <p class="card-text">
                List is Clear!!!
            </p>
        }*@
    </div>
</div>


@*<script>
    $('.unAssign').click(function (event) {

        let value = event.target.getAttribute('href');

        let isDelete = confirm("Точно хотите удалить ?"));
    if (isDelete) {
        location.href = value;
    }
    });
</script>*@